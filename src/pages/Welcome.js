import React from "react";

import Grid from "@material-ui/core/Grid";

const Welcome = () => {
  return (
    <>
      <button>Pick a Chord</button>
      <button>Pick a Scale</button>
      <button>Name a Chord</button>
    </>
  );
};

export default Welcome;
